function after(hook, param)
    --maybe have ye
    --set as value setting

    --pps_oaid_c
    --oaid
    --android_id
    --bluetooth_name (maybe)
    --advertising_id
    --local wildCards = param:getSetting('unique')
end