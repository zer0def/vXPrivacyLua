package eu.faircode.xlua;

import android.content.ContentValues;
import android.database.Cursor;

//import eu.faircode.xlua.database.IDatabaseHelper;

/*public class XGroupIO extends XGroup implements IDatabaseHelper {

    public XGroupIO(String packageName, int userId, String name, long used) {
        super(packageName, userId, name, used);
    }

    @Override
    public void readFromCursor(Cursor cursor) {

    }

    @Override
    public ContentValues createContentValues() {
        ContentValues cv = new ContentValues();
        cv.put("package", packageName);
        cv.put("uid", uid);
        cv.put("name", name);
        cv.put("used", used);
        return cv;
    }
}*/
